interface:
  display_name: "Frontend Developer"
  short_description: "Implement production frontend code from a design contract"
  default_prompt: "Use $frontend-developer to implement within the existing product system. Keep only information and components that help the intended role understand, decide, act or recover, and make every visible state match implemented behavior."
